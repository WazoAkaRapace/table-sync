# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# SQLite databases (runtime data, not committed)
data/db/

# Uploaded item illustrations (runtime data, not committed)
data/images/
*.sqlite
*.sqlite-journal
*.db

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vite
.vite/

# Docker
docker-data/

# Local AI/agent tooling (not for public repo)
.codex/
.cursor/
.agents/
.zcode/
.impeccable/
gui-test-screenshots/

# Mimosa security-hook session state (tool output, not source)
.mimosa/

# Playwright E2E: HTML report, traces, per-run seed (fresh DB per run is the contract)
playwright-report/
test-results/
e2e/.seed.json

# Marketing site: assets are copied from docs/ by scripts/serve-site.mjs
# and by the Pages workflow at deploy time (docs/ stays the single source)
site/assets/
